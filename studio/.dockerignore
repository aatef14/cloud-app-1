# Docker's build context should be as small as possible.
# This file prevents unnecessary files and folders from being sent to the Docker daemon.

# Git files
.git
.gitignore

# Node modules are installed inside the container, so we don't need to copy them.
node_modules

# Development-specific files
.env
.env.local
.env.development.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea
.vscode
README.md
